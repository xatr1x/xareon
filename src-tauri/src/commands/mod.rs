//! Tauri command handlers — the thin boundary between the frontend and the
//! application services. They wire up a repository + service per call and hold
//! no business logic of their own.

pub mod game_commands;
